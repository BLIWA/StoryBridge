// Canonical definition moved to @storybridge/site-ui — see its src/status.ts
// doc comment. Re-exported here so every existing "@/i18n/status" import in
// this app keeps working unchanged.
export { TRANSLATION_STATUS, isDraftLocale, type TranslationStatus } from "@storybridge/site-ui/status";
