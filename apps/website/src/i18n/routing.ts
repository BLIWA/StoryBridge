// Canonical definition moved to @storybridge/site-ui/navigation — see that
// file's doc comment. Re-exported here so every existing "@/i18n/routing"
// import in this app keeps working unchanged.
export { routing, isRtl, rtlLocales, type AppLocale } from "@storybridge/site-ui/navigation";
